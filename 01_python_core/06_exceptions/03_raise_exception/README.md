# 抛出异常 (Raising Exceptions)

本章节介绍如何在代码中根据逻辑需要，主动触发异常或进行断言检查。

## 核心知识点

1. **raise 语句**：用于手动抛出一个指定的异常。可以抛出内置异常，也可以自定义异常类。
2. **assert 断言**：用于在开发阶段检查某个条件是否满足，如果不满足则抛出 `AssertionError`。它通常用于捕获那些“不应该发生”的情况。

## 文件列表

- `01_raise.py`: 演示如何使用 `raise` 主动抛出异常。
- `02_assert.py`: 演示如何使用 `assert` 进行条件断言。
