# Pydantic AI 图工作流

> 本文档介绍如何使用图工作流构建复杂的多步骤应用。

## 🔄 什么是图工作流？

图工作流允许你定义复杂的、非线性的 Agent 执行流程，包括条件分支、循环和并行执行。

## 📋 基础概念

### 简单流程

```python
from pydantic_ai import Agent

# 普通 Agent：单一流程
agent = Agent('openai:gpt-4')
result = await agent.run('处理任务')
```

### 图流程

```python
# 图工作流：多步骤流程
# 1. 分析任务
# 2. 决定路径
# 3a. 路径A：执行操作1
# 3b. 路径B：执行操作2
# 4. 合并结果
```

## 🎯 使用 agent.iter()

`agent.iter()` 提供低级别的节点逐步控制：

```python
async def graph_workflow():
    async for node in agent.iter('复杂任务'):
        print(f"节点类型: {node.type}")

        if node.type == 'tool_call':
            # 可以干预工具调用
            print(f"即将调用工具: {node.tool_name}")

        elif node.type == 'complete':
            print(f"完成: {node.data}")
```

## 📚 推荐框架

对于复杂的图工作流，推荐使用 **LangGraph**：

- 更强大的图定义
- 状态管理
- 条件路由
- 持久化
- 人机交互

参考 `03_langchain_foundation/` 模块的 LangGraph 文档。

## 🔗 参考资源

- [LangGraph 文档](https://langchain-ai.github.io/langgraph/)
- [官方文档 - Graph Iteration](https://ai.pydantic.dev/agents/#graph-iteration)
