# Pydantic AI 工具系统详解

> 本文档详细介绍如何定义、注册和使用工具，让 Agent 具备执行实际操作的能力。

## 📋 目录

- [工具概述](#工具概述)
- [注册工具](#注册工具)
- [工具参数和返回值](#工具参数和返回值)
- [工具文档](#工具文档)
- [工具集](#工具集)
- [高级工具特性](#高级工具特性)
- [内置和常用工具](#内置和常用工具)
- [第三方集成](#第三方集成)

## 🔧 工具概述

### 什么是工具？

**工具（Tool）** 是 LLM 可以调用的 Python 函数，用于：
- 获取外部数据（API 调用、数据库查询）
- 执行确定性计算
- 与外部系统交互
- 验证或转换数据

### 为什么需要工具？

1. **扩展 LLM 能力**: LLM 的知识截止日期有限，工具提供实时数据
2. **确定性操作**: 数学计算、数据处理等需要精确结果
3. **外部集成**: 访问数据库、API、文件系统等
4. **降低成本**: 避免将大量上下文塞入提示

### 工具的工作流程

```
用户输入 → Agent → LLM 决策
                     ↓
            是否需要工具？
                     ↓ 是
            选择工具 + 参数
                     ↓
            执行 Python 函数
                     ↓
            结果返回给 LLM
                     ↓
            LLM 整合结果
                     ↓
            最终响应
```

## 📝 注册工具

### 方法 1: 使用装饰器 `@agent.tool`

最常用的方式：

```python
from pydantic_ai import Agent, RunContext

agent = Agent('openai:gpt-4')


@agent.tool
def get_current_time(ctx: RunContext[None]) -> str:
    """
    获取当前时间。

    Returns:
        当前时间的字符串表示
    """
    from datetime import datetime
    return datetime.now().strftime('%Y-%m-%d %H:%M:%S')


# LLM 可以自动调用这个工具
result = agent.run_sync('现在几点了？')
print(result.data)
# 输出: 现在是 2024-01-15 14:30:25
```

### 方法 2: 使用 `@agent.tool_plain`

不需要 `RunContext` 的简单工具：

```python
@agent.tool_plain
def add_numbers(a: int, b: int) -> int:
    """
    将两个数字相加。

    Args:
        a: 第一个数字
        b: 第二个数字

    Returns:
        两数之和
    """
    return a + b


result = agent.run_sync('35 加 47 等于多少？')
print(result.data)
# 输出: 35 加 47 等于 82
```

**区别**：
- `@agent.tool`: 第一个参数必须是 `RunContext`
- `@agent.tool_plain`: 不需要 `RunContext`，更简单

### 方法 3: 通过构造函数注册

适合复用工具：

```python
from pydantic_ai import Tool


def calculator(a: float, b: float, operation: str) -> float:
    """执行基本算术运算"""
    ops = {
        'add': lambda x, y: x + y,
        'subtract': lambda x, y: x - y,
        'multiply': lambda x, y: x * y,
        'divide': lambda x, y: x / y if y != 0 else float('inf')
    }
    return ops[operation](a, b)


# 创建可复用的工具
calc_tool = Tool(calculator)

# 在多个 Agent 中使用
agent1 = Agent('openai:gpt-4', tools=[calc_tool])
agent2 = Agent('anthropic:claude-3-5-sonnet', tools=[calc_tool])
```

## 🎯 工具参数和返回值

### 参数提取

Pydantic AI 自动从函数签名提取参数：

```python
@agent.tool
def search_products(
    ctx: RunContext[None],
    query: str,
    category: str | None = None,
    max_price: float = 1000.0,
    in_stock: bool = True
) -> list[dict]:
    """
    搜索产品。

    Args:
        query: 搜索关键词
        category: 产品类别（可选）
        max_price: 最高价格，默认 1000.0
        in_stock: 是否仅显示有货商品，默认 True

    Returns:
        符合条件的产品列表
    """
    # 实现搜索逻辑
    return [...]
```

**参数规则**：
- 第一个参数（`RunContext`）被跳过
- 使用 Python 类型提示
- 支持默认值
- 支持可选参数（`Optional`/`| None`）

### 支持的参数类型

```python
from typing import Literal
from pydantic import BaseModel


class SearchFilters(BaseModel):
    min_rating: float
    verified_only: bool


@agent.tool
def advanced_search(
    ctx: RunContext[None],
    # 基本类型
    query: str,
    limit: int,
    price: float,
    available: bool,

    # 枚举/字面量
    sort_by: Literal['price', 'rating', 'date'],

    # 复合类型
    filters: SearchFilters,

    # 可选类型
    category: str | None = None
) -> list[dict]:
    """高级搜索"""
    return []
```

### 返回值

工具可以返回任何 **JSON 可序列化** 的值：

```python
# 简单类型
@agent.tool_plain
def get_count() -> int:
    return 42


# 复杂类型
@agent.tool_plain
def get_user_info() -> dict[str, Any]:
    return {
        'name': 'Alice',
        'age': 30,
        'skills': ['Python', 'ML', 'Data Science']
    }


# Pydantic 模型
class UserProfile(BaseModel):
    name: str
    age: int


@agent.tool_plain
def get_profile() -> UserProfile:
    return UserProfile(name='Bob', age=25)
```

### 异步工具

支持异步函数：

```python
import httpx


@agent.tool
async def fetch_weather(ctx: RunContext[None], city: str) -> dict:
    """
    获取城市天气。

    Args:
        city: 城市名称

    Returns:
        天气信息
    """
    async with httpx.AsyncClient() as client:
        response = await client.get(
            f'https://api.weather.com/v1/current?city={city}'
        )
        return response.json()
```

## 📚 工具文档

### 文档字符串格式

Pydantic AI 支持三种文档字符串格式：

#### 1. Google Style（推荐）

```python
@agent.tool
def process_order(ctx: RunContext[None], order_id: str, action: str) -> str:
    """
    处理订单。

    Args:
        order_id: 订单 ID
        action: 操作类型（'confirm', 'cancel', 'refund'）

    Returns:
        操作结果消息

    Raises:
        ValueError: 如果操作类型无效
    """
    pass
```

#### 2. NumPy Style

```python
@agent.tool
def analyze_data(ctx: RunContext[None], data: list[float]) -> dict:
    """
    分析数据集。

    Parameters
    ----------
    data : list[float]
        数据点列表

    Returns
    -------
    dict
        包含统计信息的字典

    Notes
    -----
    使用标准差和均值进行分析
    """
    pass
```

#### 3. Sphinx Style

```python
@agent.tool
def validate_email(ctx: RunContext[None], email: str) -> bool:
    """
    验证电子邮件地址。

    :param email: 要验证的电子邮件地址
    :type email: str
    :return: 是否有效
    :rtype: bool
    """
    pass
```

### 显式指定格式

```python
from pydantic_ai import Tool

tool = Tool(
    my_function,
    docstring_format='google'  # 'google', 'numpy', 'sphinx'
)
```

### 要求参数描述

```python
tool = Tool(
    my_function,
    require_parameter_descriptions=True  # 强制每个参数都有描述
)
```

## 🗂️ 工具集

### 什么是工具集？

**工具集（Toolset）** 是一组相关工具的集合，便于管理和复用。

### 定义工具集

```python
from dataclasses import dataclass
from pydantic_ai import RunContext


@dataclass
class DatabaseTools:
    """数据库操作工具集"""

    def __init__(self, db_url: str):
        self.db_url = db_url

    def query(self, ctx: RunContext, sql: str) -> list[dict]:
        """
        执行 SQL 查询。

        Args:
            sql: SQL 查询语句

        Returns:
            查询结果
        """
        # 执行查询
        return []

    def insert(self, ctx: RunContext, table: str, data: dict) -> int:
        """
        插入数据。

        Args:
            table: 表名
            data: 要插入的数据

        Returns:
            插入的行 ID
        """
        return 0


# 使用工具集
db_tools = DatabaseTools('postgres://localhost/mydb')
agent = Agent(
    'openai:gpt-4',
    tools=[db_tools.query, db_tools.insert]
)
```

### 使用类作为工具集

```python
class WeatherTools:
    """天气相关工具"""

    def __init__(self, api_key: str):
        self.api_key = api_key

    async def current_weather(self, ctx: RunContext, city: str) -> dict:
        """获取当前天气"""
        return {}

    async def forecast(
        self,
        ctx: RunContext,
        city: str,
        days: int = 7
    ) -> list[dict]:
        """获取天气预报"""
        return []


# 注册整个类的所有方法
weather = WeatherTools(api_key='your-key')
agent = Agent(
    'openai:gpt-4',
    tools=[weather]  # 自动注册所有公共方法
)
```

## 🚀 高级工具特性

### 自定义 JSON Schema

```python
from pydantic_ai import Tool


def custom_tool(param: str) -> str:
    return param


tool = Tool(
    custom_tool,
    json_schema={
        'type': 'function',
        'function': {
            'name': 'custom_tool',
            'description': '自定义工具描述',
            'parameters': {
                'type': 'object',
                'properties': {
                    'param': {
                        'type': 'string',
                        'description': '参数描述'
                    }
                },
                'required': ['param']
            }
        }
    }
)
```

### 动态工具

运行时动态添加工具：

```python
agent = Agent('openai:gpt-4')


def add_user_tools(user_permissions: list[str]):
    """根据用户权限动态添加工具"""

    if 'admin' in user_permissions:
        @agent.tool
        def delete_user(ctx: RunContext, user_id: str) -> str:
            """删除用户"""
            return f'已删除用户 {user_id}'

    if 'editor' in user_permissions:
        @agent.tool
        def edit_content(ctx: RunContext, content_id: str) -> str:
            """编辑内容"""
            return f'已编辑内容 {content_id}'


# 使用
add_user_tools(['admin', 'editor'])
```

### 工具调用追踪

```python
@agent.tool
def tracked_tool(ctx: RunContext[None], data: str) -> str:
    """带追踪的工具"""
    print(f"工具被调用: {ctx.usage}")  # 查看 token 使用
    print(f"重试次数: {ctx.retry}")     # 查看重试信息

    return "result"
```

## 🛠️ 内置和常用工具

### 文件系统工具

```python
import os
from pathlib import Path


@agent.tool
def read_file(ctx: RunContext[None], filepath: str) -> str:
    """
    读取文件内容。

    Args:
        filepath: 文件路径

    Returns:
        文件内容
    """
    path = Path(filepath)
    if not path.exists():
        return f"文件不存在: {filepath}"

    return path.read_text()


@agent.tool
def list_directory(ctx: RunContext[None], dirpath: str = '.') -> list[str]:
    """
    列出目录内容。

    Args:
        dirpath: 目录路径

    Returns:
        文件和目录列表
    """
    return os.listdir(dirpath)
```

### HTTP 请求工具

```python
import httpx


@agent.tool
async def http_get(ctx: RunContext[None], url: str) -> dict:
    """
    发送 HTTP GET 请求。

    Args:
        url: 请求 URL

    Returns:
        响应 JSON
    """
    async with httpx.AsyncClient() as client:
        response = await client.get(url)
        return response.json()
```

### 数据库工具

```python
import sqlite3


@dataclass
class DatabaseDeps:
    db_path: str


agent = Agent('openai:gpt-4', deps_type=DatabaseDeps)


@agent.tool
def sql_query(ctx: RunContext[DatabaseDeps], query: str) -> list[dict]:
    """
    执行 SQL 查询。

    Args:
        query: SQL 查询语句

    Returns:
        查询结果
    """
    conn = sqlite3.connect(ctx.deps.db_path)
    cursor = conn.cursor()
    cursor.execute(query)

    columns = [desc[0] for desc in cursor.description]
    results = [dict(zip(columns, row)) for row in cursor.fetchall()]

    conn.close()
    return results
```

## 🔌 第三方集成

### LangChain 工具集成

```python
from langchain.tools import DuckDuckGoSearchRun


# LangChain 工具
langchain_tool = DuckDuckGoSearchRun()


# 包装为 Pydantic AI 工具
@agent.tool
def web_search(ctx: RunContext[None], query: str) -> str:
    """
    使用 DuckDuckGo 搜索网页。

    Args:
        query: 搜索查询

    Returns:
        搜索结果
    """
    return langchain_tool.run(query)
```

### MCP (Model Context Protocol) 集成

```python
from pydantic_ai.tools.mcp import MCPToolset

# 连接到 MCP 服务器
mcp_tools = MCPToolset('http://mcp-server:8080')

agent = Agent(
    'openai:gpt-4',
    tools=[mcp_tools]  # 自动注册所有 MCP 工具
)
```

## 💡 最佳实践

### 1. 清晰的文档字符串

```python
# ✅ 好的文档
@agent.tool
def process_payment(
    ctx: RunContext[None],
    amount: float,
    currency: str = 'USD'
) -> dict:
    """
    处理支付。

    Args:
        amount: 支付金额（必须为正数）
        currency: 货币代码（ISO 4217），默认 USD

    Returns:
        包含 transaction_id 和 status 的字典

    Raises:
        ValueError: 如果金额为负数
    """
    if amount <= 0:
        raise ValueError("金额必须为正数")
    return {'transaction_id': '...', 'status': 'success'}


# ❌ 差的文档
@agent.tool
def process_payment(ctx: RunContext[None], amount: float) -> dict:
    """处理支付"""
    return {}
```

### 2. 使用类型提示

```python
# ✅ 完整类型提示
@agent.tool
def fetch_user(ctx: RunContext[None], user_id: int) -> dict[str, Any]:
    return {'id': user_id, 'name': 'Alice'}


# ❌ 缺少类型提示
@agent.tool
def fetch_user(ctx, user_id):  # LLM 无法推断参数类型
    return {'id': user_id}
```

### 3. 错误处理

```python
@agent.tool
def safe_divide(ctx: RunContext[None], a: float, b: float) -> float:
    """
    安全除法。

    Args:
        a: 被除数
        b: 除数

    Returns:
        商

    Raises:
        ValueError: 如果除数为零
    """
    if b == 0:
        raise ValueError("除数不能为零")
    return a / b
```

### 4. 使用依赖注入

```python
# ✅ 通过依赖访问资源
@dataclass
class APIDeps:
    api_key: str
    base_url: str


agent = Agent('openai:gpt-4', deps_type=APIDeps)


@agent.tool
async def call_api(ctx: RunContext[APIDeps], endpoint: str) -> dict:
    """调用 API"""
    async with httpx.AsyncClient() as client:
        response = await client.get(
            f"{ctx.deps.base_url}/{endpoint}",
            headers={'Authorization': f'Bearer {ctx.deps.api_key}'}
        )
        return response.json()


# ❌ 硬编码密钥
@agent.tool
async def call_api(ctx: RunContext[None], endpoint: str) -> dict:
    api_key = 'hardcoded-key'  # 不安全
    ...
```

### 5. 限制工具调用

```python
agent = Agent(
    'openai:gpt-4',
    usage_limits=UsageLimits(
        tool_calls_limit=10  # 防止无限循环
    )
)
```

## 📚 下一步

继续学习：

1. [05_依赖注入.md](./05_依赖注入.md) - 深入理解依赖注入
2. [11_人机协作.md](./11_人机协作.md) - 工具审批机制
3. [14_最佳实践.md](./14_最佳实践.md) - 工具设计模式

## 🔗 参考资源

- [官方文档 - Tools](https://ai.pydantic.dev/tools/)
- [官方文档 - Advanced Tool Features](https://ai.pydantic.dev/tools/advanced/)
- [示例 - Tool Usage](https://github.com/pydantic/pydantic-ai/tree/main/examples)
